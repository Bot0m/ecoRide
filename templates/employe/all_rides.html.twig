{% extends 'employe/base.html.twig' %}

{% block title %}Tous les voyages{% endblock %}

{% block employe_content %}
    <!-- En-tête de bienvenue -->
    <div class="bg-gradient-to-r from-accent/20 to-accent/10 backdrop-blur-sm rounded-2xl p-6 mb-8 border border-white/20">
        <h1 class="text-title font-title text-textPrimary mb-2">Tous les voyages</h1>
        <p class="text-lg text-textSecondary">{{ totalRides }} voyage(s) au total</p>
    </div>

    <!-- Section des voyages à venir -->
    <div class="mb-12">
        <h2 class="text-subtitle font-title text-textPrimary mb-6">Voyages à venir ({{ totalUpcomingRides }})</h2>
        
        {% if upcomingRides is empty %}
            <div class="bg-gradient-to-br from-secondary/20 to-background/50 backdrop-blur-sm rounded-2xl p-8 border border-white/20 text-center">
                <div class="max-w-md mx-auto">
                    <h3 class="text-lg font-title text-textSecondary mb-2">Aucun voyage à venir</h3>
                    <p class="text-textSecondary">
                        Aucun voyage n'est prévu prochainement.
                    </p>
                </div>
            </div>
        {% else %}
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
                {% for ride in upcomingRides %}
                    <div class="bg-gradient-to-br from-accent/20 to-accent/10 backdrop-blur-sm rounded-2xl p-6 border border-accent/30 hover:border-accent/50 transition-all duration-300">
                        <div class="flex items-start justify-between mb-4">
                            <div class="flex items-center gap-3">
                                <div>
                                    <div class="flex items-center gap-2 mb-1">
                                        <h3 class="text-subtitle font-title text-textPrimary">{{ ride.departure }} → {{ ride.arrival }}</h3>
                                        {% if ride.isEcological %}
                                            <span class="bg-green-500/20 text-textSecondary text-xs px-2 py-1 rounded-full font-base border border-green-400/30">
                                                🌱 ÉCO
                                            </span>
                                        {% endif %}
                                        <span class="px-2 py-1 bg-{{ ride.status == 'actif' ? 'green' : ride.status == 'annule' ? 'red' : ride.status == 'en_cours' ? 'yellow' : 'blue' }}-500/20 text-{{ ride.status == 'actif' ? 'green' : ride.status == 'annule' ? 'red' : ride.status == 'en_cours' ? 'yellow' : 'blue' }}-400 text-xs rounded-full font-base border border-{{ ride.status == 'actif' ? 'green' : ride.status == 'annule' ? 'red' : ride.status == 'en_cours' ? 'yellow' : 'blue' }}-400/30">
                                            {{ ride.status == 'actif' ? 'ACTIF' : ride.status == 'annule' ? 'ANNULÉ' : ride.status == 'en_cours' ? 'EN COURS' : 'TERMINÉ' }}
                                        </span>
                                    </div>
                                    <div class="flex items-center gap-4 text-body text-textSecondary">
                                        <span>{{ ride.date|date('d/m/Y') }}</span>
                                        <span>{{ ride.departureTime|date('H:i') }}</span>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="space-y-3">
                            <div class="flex items-center justify-between">
                                <span class="text-body text-textSecondary">Prix par passager</span>
                                <span class="text-body font-base text-textPrimary">{{ ride.price }} crédits</span>
                            </div>
                            <div class="flex items-center justify-between">
                                <span class="text-body text-textSecondary">Places disponibles</span>
                                <span class="text-body font-base text-textPrimary">{{ ride.availableSeats }}/{{ ride.vehicle ? ride.vehicle.seats : 'N/A' }}</span>
                            </div>
                            <div class="flex items-center justify-between">
                                <span class="text-body text-textSecondary">Véhicule</span>
                                <span class="text-body font-base text-textPrimary">{{ ride.vehicle ? ride.vehicle.brand ~ ' ' ~ ride.vehicle.model : 'N/A' }}</span>
                            </div>
                            <div class="flex items-center justify-between">
                                <span class="text-body text-textSecondary">Arrivée estimée</span>
                                <span class="text-body font-base text-textPrimary">{{ ride.arrivalTime|date('H:i') }}</span>
                            </div>
                            {% if ride.date|date('Y-m-d') == "now"|date('Y-m-d') %}
                                <div class="flex items-center justify-center">
                                    <span class="bg-background/20 text-textPrimary text-xs px-3 py-1 rounded-full font-base border border-textPrimary/30">
                                        Aujourd'hui
                                    </span>
                                </div>
                            {% endif %}
                        </div>
                        
                        <!-- Informations conducteur -->
                        <div class="mt-4 pt-4 border-t border-accent/30">
                            <div class="flex items-center gap-3 mb-2">
                                <div class="w-8 h-8 rounded-full bg-gradient-to-br from-accent/40 to-accent/60 flex items-center justify-center text-white text-sm font-base">
                                    {% if ride.driver.avatar %}
                                        <img src="{{ ride.driver.avatar }}" alt="Avatar" class="w-8 h-8 rounded-full object-cover">
                                    {% else %}
                                        {{ ride.driver.pseudo|first|upper }}
                                    {% endif %}
                                </div>
                                <div>
                                    <p class="text-textPrimary font-base">{{ ride.driver.pseudo }}</p>
                                    <p class="text-textSecondary text-sm">{{ ride.driver.email }}</p>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Lien "En savoir plus" -->
                        <div class="mt-4 pt-4 border-t border-accent/30">
                            <button onclick="openRideDetailsModal({{ ride.id }})" class="inline-flex items-center text-accent hover:text-accentDark font-base transition-colors duration-300">
                                En savoir plus
                                <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                                </svg>
                            </button>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endif %}
    </div>

    <!-- Section de l'historique -->
    <div>
        <h2 class="text-subtitle font-title text-textPrimary mb-6">Historique des voyages ({{ totalPastRides }})</h2>
        
        {% if pastRides is empty %}
            <div class="bg-gradient-to-br from-secondary/20 to-background/50 backdrop-blur-sm rounded-2xl p-8 border border-white/20 text-center">
                <div class="max-w-md mx-auto">
                    <h3 class="text-lg font-title text-textSecondary mb-2">Aucun voyage passé</h3>
                    <p class="text-textSecondary">
                        L'historique des voyages apparaîtra ici une fois que des trajets auront été effectués.
                    </p>
                </div>
            </div>
        {% else %}
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
                {% for ride in pastRides %}
                    <div class="bg-gradient-to-br from-backgroundDark to-backgroundLight backdrop-blur-sm rounded-2xl p-6 border border-backgroundDark/30 hover:border-backgroundDark/50 transition-all duration-300">
                        <div class="flex items-start justify-between mb-4">
                            <div class="flex items-center gap-3">
                                <div>
                                    <div class="flex items-center gap-2 mb-1">
                                        <h3 class="text-subtitle font-title text-textPrimary">{{ ride.departure }} → {{ ride.arrival }}</h3>
                                        {% if ride.isEcological %}
                                            <span class="bg-green-500/20 text-textSecondary text-xs px-2 py-1 rounded-full font-base border border-green-400/30">
                                                🌱 ÉCO
                                            </span>
                                        {% endif %}
                                        <span class="px-2 py-1 bg-{{ ride.status == 'actif' ? 'green' : ride.status == 'annule' ? 'red' : ride.status == 'en_cours' ? 'yellow' : 'blue' }}-500/20 text-{{ ride.status == 'actif' ? 'green' : ride.status == 'annule' ? 'red' : ride.status == 'en_cours' ? 'yellow' : 'blue' }}-400 text-xs rounded-full font-base border border-{{ ride.status == 'actif' ? 'green' : ride.status == 'annule' ? 'red' : ride.status == 'en_cours' ? 'yellow' : 'blue' }}-400/30">
                                            {{ ride.status == 'actif' ? 'ACTIF' : ride.status == 'annule' ? 'ANNULÉ' : ride.status == 'en_cours' ? 'EN COURS' : 'TERMINÉ' }}
                                        </span>
                                    </div>
                                    <div class="flex items-center gap-4 text-body text-textSecondary">
                                        <span>{{ ride.date|date('d/m/Y') }}</span>
                                        <span>{{ ride.departureTime|date('H:i') }}</span>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="space-y-3">
                            <div class="flex items-center justify-between">
                                <span class="text-body text-textSecondary">Prix par passager</span>
                                <span class="text-body font-base text-textPrimary">{{ ride.price }} crédits</span>
                            </div>
                            <div class="flex items-center justify-between">
                                <span class="text-body text-textSecondary">Véhicule</span>
                                <span class="text-body font-base text-textPrimary">{{ ride.vehicle ? ride.vehicle.brand ~ ' ' ~ ride.vehicle.model : 'N/A' }}</span>
                            </div>
                            <div class="flex items-center justify-between">
                                <span class="text-body text-textSecondary">Arrivée</span>
                                <span class="text-body font-base text-textPrimary">{{ ride.arrivalTime|date('H:i') }}</span>
                            </div>
                        </div>
                        
                        <!-- Informations conducteur -->
                        <div class="mt-4 pt-4 border-t border-backgroundDark/30">
                            <div class="flex items-center gap-3 mb-2">
                                <div class="w-8 h-8 rounded-full bg-gradient-to-br from-accent/40 to-accent/60 flex items-center justify-center text-white text-sm font-base">
                                    {% if ride.driver.avatar %}
                                        <img src="{{ ride.driver.avatar }}" alt="Avatar" class="w-8 h-8 rounded-full object-cover">
                                    {% else %}
                                        {{ ride.driver.pseudo|first|upper }}
                                    {% endif %}
                                </div>
                                <div>
                                    <p class="text-textPrimary font-base">{{ ride.driver.pseudo }}</p>
                                    <p class="text-textSecondary text-sm">{{ ride.driver.email }}</p>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Lien "En savoir plus" -->
                        <div class="mt-4 pt-4 border-t border-backgroundDark/30">
                            <button onclick="openRideDetailsModal({{ ride.id }})" class="inline-flex items-center text-accent hover:text-accentDark font-base transition-colors duration-300">
                                En savoir plus
                                <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                                </svg>
                            </button>
                        </div>
                    </div>
                {% endfor %}
            </div>
            
            <!-- Pagination pour l'historique -->
            {% if totalPastPages > 1 %}
                <div class="mt-8 flex justify-center">
                    <nav class="flex items-center gap-2">
                        {% if currentPage > 1 %}
                            <a href="{{ path('employe_all_rides', {'page': currentPage - 1}) }}" class="inline-flex items-center px-3 py-2 text-sm font-base text-textSecondary bg-background/20 border border-background/30 rounded-lg hover:bg-background/40 transition-colors duration-300">
                                <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
                                </svg>
                                Précédent
                            </a>
                        {% endif %}
                        
                        <div class="flex items-center gap-1">
                            {% for page in 1..totalPastPages %}
                                {% if page == currentPage %}
                                    <span class="inline-flex items-center px-3 py-2 text-sm font-base text-textPrimary bg-accent/20 border border-accent/30 rounded-lg">
                                        {{ page }}
                                    </span>
                                {% elseif page == 1 or page == totalPastPages or (page >= currentPage - 2 and page <= currentPage + 2) %}
                                    <a href="{{ path('employe_all_rides', {'page': page}) }}" class="inline-flex items-center px-3 py-2 text-sm font-base text-textSecondary bg-background/20 border border-background/30 rounded-lg hover:bg-background/40 transition-colors duration-300">
                                        {{ page }}
                                    </a>
                                {% elseif page == currentPage - 3 or page == currentPage + 3 %}
                                    <span class="inline-flex items-center px-3 py-2 text-sm font-base text-textSecondary">
                                        ...
                                    </span>
                                {% endif %}
                            {% endfor %}
                        </div>
                        
                        {% if currentPage < totalPastPages %}
                            <a href="{{ path('employe_all_rides', {'page': currentPage + 1}) }}" class="inline-flex items-center px-3 py-2 text-sm font-base text-textSecondary bg-background/20 border border-background/30 rounded-lg hover:bg-background/40 transition-colors duration-300">
                                Suivant
                                <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                                </svg>
                            </a>
                        {% endif %}
                    </nav>
                </div>
            {% endif %}
        {% endif %}
    </div>

    <!-- Pagination pour les voyages à venir -->
    {% if totalUpcomingPages > 1 %}
        <div class="flex justify-center mt-8">
            <nav class="flex items-center gap-2">
                {% if currentPage > 1 %}
                    <a href="{{ path('employe_all_rides', {page: currentPage - 1}) }}" class="px-4 py-2 bg-secondary/70 border border-white/30 rounded-lg text-textSecondary hover:text-accent transition-colors">
                        Précédent
                    </a>
                {% endif %}
                
                {% for page in 1..totalUpcomingPages %}
                    {% if page == currentPage %}
                        <span class="px-4 py-2 bg-accent text-white rounded-lg">{{ page }}</span>
                    {% else %}
                        <a href="{{ path('employe_all_rides', {page: page}) }}" class="px-4 py-2 bg-secondary/70 border border-white/30 rounded-lg text-textSecondary hover:text-accent transition-colors">
                            {{ page }}
                        </a>
                    {% endif %}
                {% endfor %}
                
                {% if currentPage < totalUpcomingPages %}
                    <a href="{{ path('employe_all_rides', {page: currentPage + 1}) }}" class="px-4 py-2 bg-secondary/70 border border-white/30 rounded-lg text-textSecondary hover:text-accent transition-colors">
                        Suivant
                    </a>
                {% endif %}
            </nav>
        </div>
    {% endif %}
{% endblock %} 