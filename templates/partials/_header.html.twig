<nav id="navbar" class="fixed top-0 left-0 right-0 z-50 backdrop-blur-xl backdrop-saturate-150 bg-primary/80 border-b border-white/20 shadow-lg transition-all duration-300 ease-in-out">
  <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
    <!-- Logo à gauche -->
    <a href="{{ path('homepage') }}" class="flex items-center space-x-3 rtl:space-x-reverse">
        <img src="{{ asset('images/1.png') }}" class="h-8" alt="Ecoride Logo" />
        <span class="self-center text-title font-title  text-textPrimary whitespace-nowrap">Ecoride</span>
    </a>
    
    <!-- Conteneur droite : Navigation + Menu utilisateur -->
    <div class="flex items-center space-x-4">
      <!-- Navigation desktop -->
      <div class="hidden md:flex">
        <ul class="flex font-medium space-x-8 rtl:space-x-reverse">
          <li>
            <a href="{{ path('homepage') }}#qui-sommes-nous" class="block py-2 px-3 text-body font-base text-textSecondary hover:text-accentDark">Qui sommes-nous ?</a>
          </li>
          <li>
            <a href="#" class="block py-2 px-3 text-body font-base text-textSecondary hover:text-accentDark">Voyager</a>
          </li>
          <li>
            <button type="button" data-drawer-target="drawer-contact" data-drawer-show="drawer-contact" aria-controls="drawer-contact" class="block py-2 px-3 text-body font-base text-textSecondary hover:text-accentDark">Contact
            </button>
          </li>
        </ul>
      </div>
      
      <!-- Menu utilisateur -->
      <div class="flex items-center space-x-3 md:space-x-0 rtl:space-x-reverse">
        <button type="button" class="flex text-sm bg-secondary rounded-full md:me-0 focus:ring-4 focus:ring-accentDark p-2" id="user-menu-button" aria-expanded="false" data-dropdown-toggle="user-dropdown" data-dropdown-placement="bottom">
          <span class="sr-only">Open user menu</span>
          <svg class="w-6 h-6 text-accent" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" viewBox="0 0 24 24">
            <path fill-rule="evenodd" d="M12 4a4 4 0 1 0 0 8 4 4 0 0 0 0-8Zm-2 9a4 4 0 0 0-4 4v1a2 2 0 0 0 2 2h8a2 2 0 0 0 2-2v-1a4 4 0 0 0-4-4h-4Z" clip-rule="evenodd"/>
          </svg>
        </button>
        <!-- Dropdown menu -->
        <div class="z-[60] hidden my-4 text-base list-none backdrop-blur-xl bg-secondary/90 divide-y divide-white/20 rounded-2xl shadow-2xl border border-white/30" id="user-dropdown">
          {% if app.user %}
            <!-- Utilisateur connecté -->
            <div class="px-4 py-3">
              <span class="block text-body font-base text-tSecondaryDark">{{ app.user.userIdentifier }}</span>
              <span class="block text-body font-base text-tSecondaryLight truncate">{{ app.user.email|default('email@ecoride.com') }}</span>
            </div>
            <ul class="py-2" aria-labelledby="user-menu-button">

              {# Options spécifiques selon le rôle #}
              {% if is_granted('ROLE_ADMIN') %}
                <li>
                  <a href="#" class="block px-4 py-2 text-body font-base text-textSecondary hover:text-accent">Administration</a>
                </li>
                <li>
                  <a href="#" class="block px-4 py-2 text-body font-base text-textSecondary hover:text-accent">Compte employé</a>
                </li>
                <li>
                  <a href="#" class="block px-4 py-2 text-body font-base text-textSecondary hover:text-accent">Compte utilisateur</a>
                </li>
              {% elseif is_granted('ROLE_EMPLOYE') %}
                <li>
                  <a href="#" class="block px-4 py-2 text-body font-base text-textSecondary hover:text-accent">Les voyages</a>
                </li>
                <li>
                  <a href="#" class="block px-4 py-2 text-body font-base text-textSecondary hover:text-accent">Avis</a>
                </li>
              {% else %}
                {# Options pour les utilisateurs standards uniquement #}
                <li>
                  <a href="#" class="block px-4 py-2 text-body font-base text-textSecondary hover:text-accent">Crédit</a>
                </li>
                <li>
                  <a href="#" class="block px-4 py-2 text-body font-base text-textSecondary hover:text-accent">Mes voyages</a>
                </li>
                <li>
                  <a href="#" class="block px-4 py-2 text-body font-base text-textSecondary hover:text-accent">Mes véhicules</a>
                </li>
                <li>
                  <a href="#" class="block px-4 py-2 text-body font-base text-textSecondary hover:text-accent">Mes informations</a>
                </li>
              {% endif %}
              
              {# Séparateur visuel #}
              <li><hr class="border-secondaryDark my-1"></li>
              
              {# Déconnexion (pour tous) #}
              <li>
                <button type="button" onclick="confirmLogout(event)" class="block w-full text-left px-4 py-2 text-body font-base text-textSecondary hover:text-accent bg-transparent border-0 cursor-pointer">Se déconnecter</button>
              </li>
            </ul>
          {% else %}
            <!-- Utilisateur non connecté -->
            <ul class="py-2" aria-labelledby="user-menu-button">
              <li>
                <a href="{{ path('app_login') }}" class="block px-4 py-2 text-body font-base text-textSecondary hover:text-accent ">Connexion</a>
              </li>
            </ul>
          {% endif %}
        </div>
        <button data-collapse-toggle="navbar-user" type="button" class="inline-flex items-center p-2 w-10 h-10 justify-center text-sm text-accent rounded-lg md:hidden  focus:outline-none " aria-controls="navbar-user" aria-expanded="false">
          <span class="sr-only">Ouvrir le menu</span>
          <svg class="w-5 h-5" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 17 14">
              <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 1h15M1 7h15M1 13h15"/>
          </svg>
        </button>
      </div>
    </div>
  
  <!-- Navigation mobile (dropdown) -->
  <div class="z-[60] hidden absolute top-16 right-4 text-base list-none backdrop-blur-xl bg-secondary/90 divide-y divide-white/20 rounded-2xl shadow-2xl border border-white/30 md:hidden" id="navbar-user">
          <ul class="py-2">
        <li>
          <a href="{{ path('homepage') }}#qui-sommes-nous" class="block px-4 py-2 text-body font-base text-textSecondary hover:text-accentDark">Qui sommes-nous ?</a>
        </li>
      <li>
        <a href="#" class="block px-4 py-2 text-body font-base text-textSecondary hover:text-accentDark">Voyager</a>
      </li>
      <li>
        <button type="button" data-drawer-target="drawer-contact" data-drawer-show="drawer-contact" aria-controls="drawer-contact" class="block py-2 px-3 text-body font-base text-textSecondary hover:text-accentDark">Contact
        </button>
      </li>
    </ul>
  </div>
  </div>
</nav>

<!-- Modal de confirmation de déconnexion -->
<div id="logoutModal" class="fixed inset-0 backdrop-blur-sm bg-gray-600/30 overflow-y-auto h-full w-full z-[70] hidden">
  <div class="relative top-20 mx-auto p-6 border border-white/30 w-96 shadow-2xl rounded-2xl backdrop-blur-xl bg-secondary/90">
    <div class="mt-3 text-center">
      <h3 class="text-lg text-body font-subtitle text-textPrimary">Confirmation de déconnexion</h3>
      <div class="mt-2 px-7 py-3">
        <p class="text-sm text-body font-base text-textSecondary">
          Êtes-vous sûr de vouloir vous déconnecter ?
        </p>
      </div>
      <div class="items-center px-4 py-3">
        <button id="confirmLogoutBtn" class="font-body text-base text-textSecondary hover:text-textPrimary bg-accent/80 hover:bg-accent backdrop-blur-sm rounded-xl text-sm px-6 py-3 mx-2 transition-all duration-300">
          Oui
        </button>
        <button id="cancelLogoutBtn" class="font-body text-base text-textSecondary hover:text-textPrimary bg-secondary/60 hover:bg-secondary backdrop-blur-sm border border-white/30 rounded-xl text-sm px-6 py-3 mx-2 transition-all duration-300">
          Non
        </button>
      </div>
    </div>
  </div>
</div>

<script>
function confirmLogout(event) {
    event.preventDefault();
    document.getElementById('logoutModal').classList.remove('hidden');
}

document.getElementById('confirmLogoutBtn').addEventListener('click', function() {
    window.location.href = "{{ path('app_logout') }}";
});

document.getElementById('cancelLogoutBtn').addEventListener('click', function() {
    document.getElementById('logoutModal').classList.add('hidden');
});

// Fermer la modal en cliquant en dehors
document.getElementById('logoutModal').addEventListener('click', function(event) {
    if (event.target === this) {
        this.classList.add('hidden');
    }
});

// Fermer le menu mobile quand on clique sur un lien
document.addEventListener('DOMContentLoaded', function() {
    const mobileMenu = document.getElementById('navbar-user');
    const mobileMenuLinks = mobileMenu.querySelectorAll('a, button');
    
    mobileMenuLinks.forEach(function(link) {
        link.addEventListener('click', function() {
            // Fermer le menu mobile
            mobileMenu.classList.add('hidden');
        });
    });
});

// Effet de réduction de la navbar au scroll
document.addEventListener('DOMContentLoaded', function() {
    const navbar = document.getElementById('navbar');
    const navbarContainer = navbar.querySelector('.max-w-screen-xl');
    
    let lastScrollTop = 0;
    
    window.addEventListener('scroll', function() {
        let scrollTop = window.pageYOffset || document.documentElement.scrollTop;
        
        if (scrollTop > 80) {
            // Navbar réduite
            navbarContainer.classList.remove('p-4');
            navbarContainer.classList.add('p-2');
            navbar.classList.add('backdrop-blur-2xl', 'bg-primary/60');
            navbar.classList.remove('backdrop-blur-xl', 'bg-primary/80');
        } else {
            // Navbar normale
            navbarContainer.classList.remove('p-2');
            navbarContainer.classList.add('p-4');
            navbar.classList.remove('backdrop-blur-2xl', 'bg-primary/60');
            navbar.classList.add('backdrop-blur-xl', 'bg-primary/80');
        }
        
        lastScrollTop = scrollTop;
    });
});
</script>
